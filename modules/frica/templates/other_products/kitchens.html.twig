<?php
?>
<style>

</style>
{{ attach_library('shop/computers-to-end-styling') }}
<!-- section start -->
<div class="computers_section layout_padding">
  <div class="container">
    <h1 class="computers_taital">Kitchen</h1>
  </div>
</div>
<div class="computers_section_2">
  <div class="container-fluid">
    <div class="computer_main">
      <div class="row">
        {% for kitchen in data.kitchens %} {#data.productsForCurrentPage#}
          <div class="col-md-4">
            <div class="computer_img"><a href="{{ url('frica.product_detail', {'idSP': kitchen.idSP}) }}"><img src="{{ file_url(directory ~ '/images/' ~ kitchen.img) }}"></a></div>
            <h4 class="computer_text">{{ kitchen.idLSP }}</h4>
            <div class="computer_text_main">
              <h4 class="dell_text"><a href="{{ url('frica.product_detail', {'idSP': kitchen.idSP}) }}">{{ kitchen.name }}</a></h4>
              <h6 class="price_text">${{ kitchen.promotional_price|number_format(0, '.', ',') }}</h6>
              <h6 class="price_text_1">${{ kitchen.price|number_format(0, '.', ',') }}</h6>
            </div>
            <div class="cart_bt_1"><a href="{{ url('frica.cart', {'idSP': kitchen.idSP}) }}">Add To Cart</a></div>
          </div>
        {% endfor %}
      </div>
    </div>
  </div>
</div>
<!-- section end -->

