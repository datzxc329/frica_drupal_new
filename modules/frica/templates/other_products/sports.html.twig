<?php
?>
{{ attach_library('shop/computers-to-end-styling') }}
<!-- section start -->
<div class="computers_section layout_padding">
  <div class="container">
    <h1 class="computers_taital">Sport</h1>
  </div>
</div>
<div class="computers_section_2">
  <div class="container-fluid">
    <div class="computer_main">
      <div class="row">
        {% for sport in data.sports %} {#data.productsForCurrentPage#}
          <div class="col-md-4">
            <div class="computer_img"><a href="{{ url('frica.product_detail', {'idSP': sport.idSP}) }}"><img src="{{ file_url(directory ~ '/images/' ~ sport.img) }}"></a></div>
            <h4 class="computer_text">{{ sport.idLSP }}</h4>
            <div class="computer_text_main">
              <h4 class="dell_text"><a href="{{ url('frica.product_detail', {'idSP': sport.idSP}) }}">{{ sport.name }}</a></h4>
              <h6 class="price_text">${{ sport.promotional_price|number_format(0, '.', ',') }}</h6>
              <h6 class="price_text_1">${{ sport.price|number_format(0, '.', ',') }}</h6>
            </div>
            <div class="cart_bt_1"><a href="{{ url('frica.cart', {'idSP': sport.idSP}) }}">Add To Cart</a></div>
          </div>
        {% endfor %}
      </div>
    </div>
  </div>
</div>
<!-- section end -->

