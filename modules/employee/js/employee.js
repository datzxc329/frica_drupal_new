/**
 * @file
 * employee behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.employeeEmployee = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };
  new DataTable('#example');
} (Drupal));
